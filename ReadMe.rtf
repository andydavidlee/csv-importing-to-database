Summary
Upload and open the views/index.ejs to open the home page. A datatable will load where you can filter and calculate sum totals of the value's table.

How it works. 

Load in the .csv file, it will then convert the information into JSON and load it into a new file. From there the program will read the info and render it to the browser.

This app is programmed with:

- Node.js with Express, and
- Jquery

Add ons include:

- Bootstrap, and
- Datatables.